import React from 'react';
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({

}));

export default function WorkingSpaceDashBoard() {
    const classes = useStyles();

    return (
        <div>Workspace</div>
    )
}